# Shared Layer

- Owns generic browser utilities, generic UI building blocks, and reusable scene primitives.
- Must stay feature-neutral.
- Must not import from `features/`.

Current ownership:
- Safe browser access utilities
- Shared workflow scene canvas shell

