<Layouts.app
  flash={@flash}
  current_scope={@current_scope}
  current_path={~p"/projects/#{@project_id}/workspaces"}
>
  <:page_header>
    <div class="space-y-2">
      <p class="text-xs font-semibold uppercase tracking-[0.2em] text-muted">project</p>
      <h1 class="text-3xl font-semibold">Workspaces</h1>
      <p class="text-sm text-muted">
        Sleep-first execution environments scoped to this project.
      </p>
    </div>
  </:page_header>

  <div id="workspaces-index-page" class="grid gap-6 lg:grid-cols-[minmax(0,1fr)_20rem]">
    <section class="rounded-box border border-base-300 bg-base-100 p-4 sm:p-6">
      <div class="mb-4 flex items-center justify-between gap-3">
        <h2 class="text-lg font-semibold">Workspaces</h2>
        <span class="badge badge-outline">project {@project_id}</span>
      </div>

      <div id="workspaces-list" phx-update="stream" class="space-y-3">
        <div class="hidden only:block rounded-box border border-base-300 bg-base-200/70 p-4 text-sm text-muted">
          No workspaces in this project yet.
        </div>

        <article
          :for={{dom_id, workspace} <- @streams.workspaces}
          id={dom_id}
          class="rounded-box border border-base-300 bg-base-100 p-4"
        >
          <div class="flex items-center justify-between gap-3">
            <div>
              <p class="font-medium text-base-content">{workspace.name}</p>
              <p class="text-xs text-muted">{workspace.remote_name}</p>
            </div>
            <span class="badge badge-ghost">{workspace.status}</span>
          </div>

          <div class="mt-3 flex items-center gap-2">
            <.link
              id={"workspace-open-#{workspace.id}"}
              navigate={~p"/projects/#{@project_id}/workspaces/#{workspace.id}"}
              class="btn btn-primary btn-sm"
            >
              Open
            </.link>
            <button
              id={"workspace-delete-#{workspace.id}"}
              type="button"
              class="btn btn-outline btn-error btn-sm"
              phx-click="delete_workspace"
              phx-value-id={workspace.id}
            >
              Delete
            </button>
          </div>
        </article>
      </div>
    </section>

    <aside class="rounded-box border border-base-300 bg-base-100 p-4 sm:p-6">
      <h2 class="text-lg font-semibold">Create Workspace</h2>
      <p class="mt-1 text-sm text-muted">Provision a new isolated microVM in this project.</p>

      <.form
        for={@create_form}
        id="workspaces-create-form"
        phx-change="validate_create_workspace"
        phx-submit="create_workspace"
        class="mt-4 space-y-4"
      >
        <.input
          field={@create_form[:name]}
          type="text"
          label="Name"
          placeholder="build-agent"
          required
        />

        <button id="workspaces-create-submit" type="submit" class="btn btn-primary w-full">
          Create Workspace
        </button>
      </.form>
    </aside>
  </div>
</Layouts.app>
